# Per-user RSS collection for a Linux login node.
#
# Install the collector as /etc/zabbix/scripts/zabbix-user-memory and make it
# executable. This key returns one JSON snapshot; the template derives all
# discovery and per-user values from it.
UserParameter=user.memory.get,/etc/zabbix/scripts/zabbix-user-memory collect
